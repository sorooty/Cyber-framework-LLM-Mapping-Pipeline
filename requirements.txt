# Déjà présents dans le venv
pandas>=3.0.1
numpy>=2.4.2
openpyxl>=3.1.5
pyyaml>=6.0.3

# Étape 2 — embeddings & similarité sémantique
sentence-transformers
scipy

# Étape 3 — visualisation matrice de corrélation
seaborn
matplotlib

# Étape 4 — validation LLM output & appels API
pydantic>=2.0
openai
